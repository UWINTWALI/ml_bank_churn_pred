CREATE TABLE customers (
    RowNumber INTEGER,
    CustomerId BIGINT,
    Surname TEXT,
    CreditScore INTEGER,
    Geography TEXT,
    Gender TEXT,
    Age INTEGER,
    Tenure INTEGER,
    Balance NUMERIC,
    NumOfProducts INTEGER,
    HasCrCard INTEGER,
    IsActiveMember INTEGER,
    EstimatedSalary NUMERIC,
    Exited INTEGER
);
